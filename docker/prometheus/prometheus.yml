global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "agentrec"
    metrics_path: "/api/v1/metrics"
    static_configs:
      - targets: ["app:8000"]
        labels:
          service: "agentrec"
